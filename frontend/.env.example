# Leave the API path as /api when using the Vite proxy or Docker setup.
VITE_API_BASE_URL=/api
VITE_AUTH_PROVIDER=backend

# Reserved for the Supabase Auth/data migration.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
